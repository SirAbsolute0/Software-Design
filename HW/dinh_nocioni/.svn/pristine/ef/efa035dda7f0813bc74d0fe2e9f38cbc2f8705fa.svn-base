import requests

def getResponse(word):
    URL = f"http://agilec.cs.uh.edu/spell?check={word}"
    return requests.get(URL).text

def parse(response):
    if response == "true":
        return True
    else: return False

