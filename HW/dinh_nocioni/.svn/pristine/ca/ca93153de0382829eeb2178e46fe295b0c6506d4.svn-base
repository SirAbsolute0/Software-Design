import requests

def getResponse(word):
    URL = f"http://agilec.cs.uh.edu/spell?check={word}"
    return requests.get(URL).text

def parse(response): #Feedback: let's reduce code in this function
    if response == "true":
        return True
    else: return False

