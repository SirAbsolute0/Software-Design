def fibonacci(position):
    if(position < 0):
        raise ValueError("Position must be a positive integer") 
    
    previous = 1
    current = previous #Feedback: we can merge this with the previous line
    
    for i in range(position - 1): #Feedback: let's reduce code within the loop
        current = current + previous
        previous = current - previous
        
    return current

